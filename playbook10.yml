---
- name: Host vars variable configuration
  hosts: 172.31.6.177
  tasks:
    - name: user creation
      user:
        name: "{{a}}"
        password: "{{b}}"
        uid: "{{c}}"
        home: "{{d}}"
        shell: "{{e}}"
...

